<!DOCTYPE html>
<html>
<head>
  <title >ForkStackoverflow</title>
  <%= stylesheet_link_tag    "reset", "grid", "base" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
  <body>
    <!-- .wrapper -->
    <div class="wrapper">
      <!-- .container -->
      <div class="container">
      <div id="user_nav">
        <% if user_signed_in? %>
          Te has logeado con la cuenta <%= current_user.email %>. no eres tu?
          <%= link_to "deslogear", destroy_user_session_path %>
        <% else %>
          <%= link_to "Registrarse", new_user_registration_path %> o <%= link_to "logearse", new_user_session_path %>
        <% end %>
      </div><br/>
        <div class="header row">
          <div class="four columns">
            <h1 id="title"  >Fork Stack Overflow</h1>
          </div>
        </div>

        <div class="flashes">
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
        </div>

        <%= yield %>
        <!-- .footer -->
        <div class="footer">
          site design / logo © 2010 stack overflow internet services, inc; user contributions licensed under cc-wiki with attribution required
        </div><!-- .footer -->
      </div><!-- .container -->
    </div><!-- .wrapper -->
  </body>
</html>

